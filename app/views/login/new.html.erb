<div class="emp_container container-fluid text-center">
  <div class="row content">
    <div class="col-sm-4 sidenav">
    </div>
    <div class="emp_form col-sm-4 text-left">
      <div class="form_cont">
      <h1>Welcome</h1>
      <h2>Login</h2>
      <% if flash[:messages] %>
        <p><%= flash[:messages] %></p>
      <% end %>
      <%= form_tag "/login" do%>
        <div class="form-group">
          <%= label_tag :username %>
          <%= text_field_tag :username, "", :class => "form-control", placeholder: 'Username' %>
        </div>
        <div class="form-group">
          <%= label_tag :password %>
          <%= password_field_tag :password, "", :class => "form-control", placeholder: 'Password' %>
        </div>
        <div class="collection_label">
          <%= collection_radio_buttons nil, :account, ["Manager", "Employee"], :itself, :itself  %>
        </div>
        <%= submit_tag "Submit", :class => "btn btn-primary"%>
      <% end %>
      </div>
    </div>
    <div class="col-sm-4 sidenav">
    </div>
  </div>
</div>
